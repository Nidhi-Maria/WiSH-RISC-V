#include <stdio.h>

int main() {
    // printf prints text inside quotes to the screen
    int a = 0;
    int b = 1000;
    int c = a * b;
    int z = c + 1;
    // return 0 indicates successful execution
    return 0;
}
